# abort script on any command that exits with a non zero value
set -e -x

dest=${BOSH_INSTALL_TARGET}/var/vcap/bin
name=sauron

mkdir -p $dest

# move sauron module
cp ./sauron/sauron_v1.0.bin $dest/$name

chown syslog:syslog $dest/$name
chmod 0755 $dest/$name

